import styled from "styled-components/native";

import { vh, vw } from "../../../helpers/responsive";

export const SPOT_PADDING = 1.15;

export const OverlayView = styled.View`
  height: ${vh(100)};
  width: ${vw(100)};
`;
