{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./build",
    "typeRoots": [
      "../node_modules/@types",
      "./typings"
    ]
  },
  "exclude": [
    "build/*",
    "dist/*"
  ]
}
